dmaven은 빌드 도구입니다.
maven은 의존성(dependency) 관리를 합니다.

다운로드 받은 jar는 ~/.m2/repository 폴더에 캐싱됩니다.
다운받는 jar는 2종류로 나뉩니다.

maven phase, goal(mvn spring-boot:run)을 수행하는 것
프로젝트의 pom.xml 에 있는 앱을 위한 jar
이렇게 두 종류로 나뉘죠.
